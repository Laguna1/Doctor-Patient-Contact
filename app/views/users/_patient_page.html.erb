<p><%= link_to 'My Doctors List' %> (<%= @user.doctors.count %>)</p>
    <% @user.doctors.each do |doctor| %>
     <p>* <%= doctor.fullname %> 
          <%= button_to 'Unbook Doctor', unbook_doctor_path(doctor) %>
    </p>
    <% end %>